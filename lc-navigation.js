/* Navigation only. No form events, pricing, storage or network access. */
(()=>{'use strict';const button=document.querySelector('.lc-menu-toggle'),nav=document.querySelector('.lc-navigation');if(!button||!nav)return;const close=()=>{nav.classList.remove('is-open');button.setAttribute('aria-expanded','false');};button.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')!=='true';button.setAttribute('aria-expanded',String(open));nav.classList.toggle('is-open',open);});nav.addEventListener('click',event=>{if(event.target.closest('a'))close();});document.addEventListener('keydown',event=>{if(event.key==='Escape'&&button.getAttribute('aria-expanded')==='true'){close();button.focus();}});matchMedia('(min-width:801px)').addEventListener('change',close);})();
